<header id="beginning">
    <div class="container-fluid">
        <div class="row justify-content-between align-items-center">
            <a href="{{ path('app_home') }}" class="col-3 my-1 bear-link"><img class="bear w-100" src="{{asset('images/bear_hello.jpg')}}" alt="bear say welcome"></a>
            <div class="col d-flex flex-column flex-sm-row align-items-center justify-content-between mb-1">
                <h1 class="text-center"><a href="{{ path('app_home')}}"><span class="text-uppercase">adm</span></a></h1>
                <div>
                    {% if app.user %}
                    <nav class="navbar">
                        <ul>
                            <a class={{include('layouts/_includes/actual.html.twig', {'route': 'app_home'})}} 
                               href="{{ path('app_home') }}">accueil</a>
                            <a class={{include('layouts/_includes/actual.html.twig', {'route': 'app_user_read'})}} 
                               href="{{ path('app_user_read', {id: app.user.id}) }}">profil</a>
                            <a class="btn btn-info btn-sm" 
                               href="{{ path('app_logout') }}" >déconnexion</a>
                            {% if is_granted("ROLE_ADMIN") %}
                                <a class={{include('layouts/_includes/actual.html.twig', {'route': 'app_user_research'})}} 
                                   href="{{ path('app_user_research') }}" >utilisateurs</a>
                            {% endif %}
                            {% else %}
                            <a class="btn btn-info btn-sm" href="{{ path('app_login', {id: 0}) }}" >connexion</a>
                        </ul>
                    </nav>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</header>