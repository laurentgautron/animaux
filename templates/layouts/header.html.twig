<header>
    <div class="container-fluid">
        <div class="row justify-content-between align-items-center">
            <a href="{{ path('app_home') }}" class="col-3 my-1 bear-link"><img class="bear w-100" src="{{asset('images/bear_hello.jpg')}}" alt="bear say welcome"></a>
            <div class="col d-flex flex-column flex-sm-row align-items-center justify-content-between mb-1">
                <h1 class="text-center"><span class="text-uppercase">adm</span></h1>
                <div class="">
                    {% if app.user %}
                        <a class="btn btn-info btn-sm" href="{{ path('app_user_read', {id: app.user.id}) }}">Profil</a>
                        <a class="btn btn-info btn-sm" href="{{ path('app_logout') }}" >Deconnexion</a>
                        {% if is_granted("ROLE_ADMIN") %}
                            <a class="btn btn-info btn-sm" href="{{ path('app_user_research') }}" >Utilisateurs</a>
                        {% endif %}
                        {% else %}
                        <a class="btn btn-info btn-sm" href="{{ path('app_login') }}" >Connexion</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</header>