<header>
    <div class="container-fluid">
        <div class="row justify-content-between align-items-center">
            <img class="col col-sm-2 my-1 bear" src="{{asset('images/bear_hello.jpg')}}" alt="bear say welcome">
            <h1 class="col text-center">
                {% if mobile %}
                <span class="text-uppercase">adm</span>
                {% else %}
                <span class="text-capitalize">animaux du monde</span>
                {% endif %}
            </h1>
            <div class="col cta">
                {% if app.user %}
                    <a class="btn btn-info btn-sm" href="{{ path('app_logout') }}" >Deconnexion</a>
                    {% if is_granted("ROLE_ADMIN") %}
                        <a class="col btn btn-info btn-sm" href="{{ path('app_user') }}" >Utilisateurs</a>
                    {% endif %}
                    {% else %}
                    <a class="btn btn-info btn-sm mx-3" href="{{ path('app_login') }}" >Connexion</a>
                {% endif %}
            </div>
        </div>
    </div>
</header>