{% extends 'layouts/base.html.twig' %}

{% block title %}Enregistrer{% endblock %}

{% block body %}
    {{ form_start(registrationForm) }}
    <div class="row flex-column align-items-center">
        <h1 class='col text-center my-3'>Enregistrer un(e) nouvel(le) utilisateur(trice)</i></h1>
        <div class="col-sm-8">
            {{ include('layouts/_includes/form_fields.html.twig', {'labelName': 'Nom', 'formField': registrationForm.firstName}) }}
            {{ include('layouts/_includes/form_fields.html.twig', {'labelName': 'Prénom', 'formField': registrationForm.lastName}) }}
            {{ include('layouts/_includes/form_fields.html.twig', {'labelName': 'Rôle', 'formField': registrationForm.roles}) }}
            {{ include('layouts/_includes/form_fields.html.twig', {'labelName': 'Email', 'formField': registrationForm.email}) }}
            {{ include('layouts/_includes/form_fields.html.twig', {'labelName': 'Mot de Passe', 'formField': registrationForm.plainPassword}) }}
            {{ form_row(registrationForm.agreeTerms) }}
            <button class="btn bg-info mt-3 w-100">Enregistrer</button>
        </div>
    </div>
	{{ form_end(registrationForm) }}
    {# <h1 class='mt-3'>Enregistrer un utilisateur</h1>

    {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.firstName) }}
        {{ form_row(registrationForm.lastName) }}
        {{ form_row(registrationForm.role) }}
        {{ form_row(registrationForm.email) }}
        {{ form_row(registrationForm.plainPassword) }}
        {{ form_row(registrationForm.agreeTerms) }}

        <button type="submit" class="btn register">Enregister</button>
    {{ form_end(registrationForm) }} #}
{% endblock %}
